# AWS VPC module

Provisions the AWS VPC instance


## Software dependencies

The module depends on the following software components:

### Terraform version

- \>= v0.15

### Terraform providers


- aws (hashicorp/aws)

### Module dependencies


None

## Example usage

```hcl
module "aws-vpc" {
  source = "github.com/cloud-native-toolkit/terraform-aws-vpc"

  enable_dns_hostnames = var.aws-vpc_enable_dns_hostnames
  enable_dns_support = var.aws-vpc_enable_dns_support
  external_cidr = var.aws-vpc_external_cidr
  instance_tenancy = var.aws-vpc_instance_tenancy
  internal_cidr = var.aws-vpc_internal_cidr
  name = var.aws-vpc_name
  name_prefix = var.name_prefix
  provision = var.aws-vpc_provision
  resource_group_name = var.resource_group_name
}

```

## Module details

### Inputs

| Name | Description | Required | Default | Source |
|------|-------------|---------|----------|--------|
| name | The name of the VPC instance | true |  |  |
| internal_cidr | The cidr range of the internal network for the AWS VPC. Either provide manually or chose from AWS IPAM poolsß | false | 10.0.0.0/16 |  |
| external_cidr | The cidr range of the external network for the AWS VPC. | false | 0.0.0.0/0 |  |
| provision | Flag indicating that the instance should be provisioned. If false then an existing instance will be looked up | false | true |  |
| instance_tenancy | Instance is shared / dedicated, etc. #[default, dedicated, host] | false | default |  |
| name_prefix | Prefix to be added to the names of resources which are being provisioned | false | swe |  |
| enable_dns_support | default is true. [true, false]] | false | true |  |
| enable_dns_hostnames | Default is true. [true, false]]. ROSA cluster requires this to be set as true | false | true |  |
| resource_group_name | The name of the resource group where the VPC is deployed. On AWS this value becomes a tag. | false | default |  |

### Outputs

| Name | Description |
|------|-------------|
| vpc |  |
| vpc_name |  |
| vpc_id | The id for the vpc network |
| vpc_arn | The arn of the vpc |
| default_network_acl_id | The id for the default network acl |
| default_security_group_id | The id of the default security group. |
| default_security_group_arn | The arn of the default security group. |
| vpc_cidr | The internal vpc cidr. |

## Resources

- [Documentation](https://operate.cloudnativetoolkit.dev)
- [Module catalog](https://modules.cloudnativetoolkit.dev)

> License: Apache License 2.0 | Generated by iascable (0.1.5)
